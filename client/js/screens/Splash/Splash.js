import React, { Component } from "react";
import { Text, View } from "react-native";

export default () => {
      return(
        <View styles={styles.styles}>
            <Text>
                Loading Out Loud!
            </Text>
        </View>);
}